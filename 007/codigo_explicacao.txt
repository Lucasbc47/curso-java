int size = 27;
// Declara variavel "size" do tipo inteiro e atribui numero 27. 

String name = "Fido";
// Declara variavel "name" do tipo String e atribui texto "Fido".

Dog myDog = new Dog(name, size);
// Cria objeto Dog com nome de myDog e atribui argumentos name e size.

x = size - 5;
// Atribui varivel x uma subtração do valor size - 5.

if (x < 15){ 
// Cria um if se caso o valor x for menor que 15
   myDog.bark(8)
   // Chama função bark e passa argumento 8 do myDog 
}

int[] numlist = {2, 4, 6, 8};
// Declara um array de inteiros com nome de numlist e atribui: 2, 4, 6 e 8

System.out.println("Hello");
// Imprime "Hello" a tela

System.out.println("Dog" + name);
// Imprime "Dog" e variavel name a tela

String num = "8";
// Declara variavel "num" do tipo String e atribui texto "8"

int x = integer.parseInt(num);
// Declara variavel "x" do tipo inteiro e converta o tipo do num com a função parseInt() atribuindo o num

try {
// Cria uma condição try
   readTheFile("myFile.txt");
   // Chama função readTheFile e atribui argumento "myFile.txt"'
}

catch(FileNotFoundException ex) {
// Capture a exceção FileNotFoundException 
   System.out.println("File not found");
   // Imprime "File not found"
}

   
